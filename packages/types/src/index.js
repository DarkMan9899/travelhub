/**
 * @travelhub/types — public entry point.
 *
 * INTENTIONALLY EMPTY IN SPRINT 1.
 *
 * This package will hold shared JSDoc @typedef definitions mirroring the
 * resource shapes in API_SPECIFICATION.md and OPENAPI_SCHEMA_REFERENCE.md
 * (Booking, Listing, Payment, etc.) so apps/web and apps/api can both
 * import the same type documentation instead of two drifting copies.
 *
 * Sprint 1 is foundation-only — no API resources have been implemented
 * yet, so there is nothing real to type. The first module to implement a
 * real endpoint (a later sprint) adds its corresponding typedef file here
 * in the same pull request, never after the fact.
 */
module.exports = {};
