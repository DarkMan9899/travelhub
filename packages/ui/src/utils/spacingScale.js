/**
 * Shared spacing-scale keys — mirrors mixins/_gap-scale.scss's
 * `$gap-scale` map key-for-key. Used by Stack/Inline/Grid's PropTypes so
 * the valid-values list is defined once, not duplicated per component.
 */
const SPACING_SCALE = ['0', '1', '2', '3', '4', '6', '8', '12', '16', '24'];

export default SPACING_SCALE;
