/**
 * `partner-onboarding` module public export surface
 * (FRONTEND_ARCHITECTURE.md §6.2) — the ONLY entry point other modules/
 * pages may import from (§6.3). P1.2 (Master Roadmap).
 */

export { default as PartnerApplicationPageContent } from './components/PartnerApplicationPageContent/PartnerApplicationPageContent.jsx';
